"""sgpg: GPG-encrypted messaging over Signal.

GPG owns keys and cryptography. Signal is transport only. This package
never implements cryptographic primitives and never persists plaintext.
"""

__version__ = "0.1.0"
